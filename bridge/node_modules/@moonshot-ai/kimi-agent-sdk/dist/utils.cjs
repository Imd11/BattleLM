'use strict';

// utils.ts
function extractBrief(display) {
  const brief = display?.find((d) => d.type === "brief");
  return brief?.text ?? "";
}
function collectText(events) {
  return events.filter((e) => e.type === "ContentPart" && "payload" in e && e.payload.type === "text").map((e) => e.payload.text).join("");
}
function extractTextFromContentParts(parts) {
  return parts.filter((p) => p.type === "text").map((p) => p.text).join("\n");
}
function formatContentOutput(output) {
  if (typeof output === "string") {
    return output;
  }
  if (!Array.isArray(output)) {
    return JSON.stringify(output);
  }
  return output.map((item) => {
    if (typeof item === "string") {
      return item;
    }
    if (item.type === "text") {
      return item.text;
    }
    return `[${item.type}]`;
  }).filter(Boolean).join("\n");
}

exports.collectText = collectText;
exports.extractBrief = extractBrief;
exports.extractTextFromContentParts = extractTextFromContentParts;
exports.formatContentOutput = formatContentOutput;
//# sourceMappingURL=utils.cjs.map
//# sourceMappingURL=utils.cjs.map