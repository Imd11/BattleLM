import { DisplayBlock, ContentPart, StreamEvent } from './schema.mjs';
import 'zod';

declare function extractBrief(display?: DisplayBlock[]): string;
/** Collect all text content from stream events */
declare function collectText(events: StreamEvent[]): string;
declare function extractTextFromContentParts(parts: ContentPart[]): string;
declare function formatContentOutput(output: string | ContentPart[]): string;

export { collectText, extractBrief, extractTextFromContentParts, formatContentOutput };
